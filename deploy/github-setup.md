GitHub Setup Steps
github.com → New Repository → beko-agent-core
Copy HTTPS URL
In PowerShell:
text
git init
git remote add origin https://github.com/YOUR_USERNAME/beko-agent-core.git
git add .
git commit -m "Initial BEKO Agent v1.2"
git branch -M main
git push -u origin main
text