{
  "thought": "Create a Meta Ads optimizer for DZD COD campaigns",
  "steps": [
    {
      "action": "write_file",
      "path": "SKILL.md",
      "content": "# Meta Ads Optimizer Skill\n## Overview\nThis skill is designed to optimize Meta Ads for DZD COD campaigns, with the goal of scaling to 5000 DZD daily and achieving 20-40% returns.\n## Requirements\n* Python 3.8+\n* Meta Ads API access\n* TRC Protocol integration\n## Usage\n1. Install required libraries: `pip install -r requirements.txt`\n2. Set up Meta Ads API credentials: `export METAADS_API_KEY=YOUR_API_KEY`\n3. Run the optimizer: `python skills/meta_ads_optimizer.py`"
    },
    {
      "action": "write_file",
      "path": "evals/evals.json",
      "content": "[\n  {\n    \"test_case\": \"Low budget campaign\",\n    \"budget\": 1000,\n    \"expected_return\": 200\n  },\n  {\n    \"test_case\": \"Medium budget campaign\",\n    \"budget\": 2500,\n    \"expected_return\": 500\n  },\n  {\n    \"test_case\": \"High budget campaign\",\n    \"budget\": 5000,\n    \"expected_return\": 1000\n  }\n]"
    },
    {
      "action": "write_file",
      "path": "scripts/meta-ads.py",
      "content": "import os\nimport json\nfrom facebookads.adobjects.adaccount import AdAccount\nfrom facebookads.adobjects.campaign import Campaign\n\n# Set up Meta Ads API credentials\napi_key = os.environ['METAADS_API_KEY']\n\n# Define a function to create a new campaign\ndef create_campaign(budget, return_on_ad_spend):\n  ad_account = AdAccount('act_4330803053874384')\n  campaign = Campaign(parent_id='act_4330803053874384')\n  campaign[Campaign.Field.name] = 'Meta Ads Optimizer Campaign'\n  campaign[Campaign.Field.objective] = 'CONVERSIONS'\n  campaign[Campaign.Field.status] = 'ACTIVE'\n  campaign[Campaign.Field.budget] = {'budget': budget}\n  campaign.remote_create(params={'access_token': api_key})\n  return campaign\n\n# Define a function to optimize a campaign\ndef optimize_campaign(campaign, return_on_ad_spend):\n  # TO DO: implement optimization logic using TRC Protocol\n  pass"
    },
    {
      "action": "write_file",
      "path": "skills/meta_ads_optimizer.py",
      "content": "import json\nfrom scripts.meta_ads import create_campaign, optimize_campaign\n\n# Load evaluation cases\nwith open('evals/evals.json') as f:\n  eval_cases = json.load(f)\n\n# Run the optimizer for each evaluation case\nfor case in eval_cases:\n  budget = case['budget']\n  expected_return = case['expected_return']\n  campaign = create_campaign(budget, expected_return)\n  optimize_campaign(campaign, expected_return)"
    }
  ]
}