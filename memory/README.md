# Memory Folder
This folder is used by BEKO Agent to store simple memory data, including learned facts, notes, and timestamps.
It is a lightweight project memory and not used for model training.
BEKO Agent should use this folder to store and retrieve information that is useful for its operation.